fn main() {
    let input = include_str!("./input.txt");
    let output = solve(input);
    dbg!(output);
}

fn pre_process(input: &str) {

}

fn solve(input: &str) -> String {
    pre_process(input);
    input.to_string()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn solve_puzzle() {
        let result = solve("");
        assert_eq!(result, "");
    }
}